put:
  summary: 发布
  tags:
    - 词条
  parameters:
    - in: path
      name: entry_id
      required: true
      schema:
        type: integer
        example: 1
  responses:
    200:
      $ref: '../responses/200.yaml'
