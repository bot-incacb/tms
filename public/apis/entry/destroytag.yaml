delete:
  summary: '删除标签'
  tags:
    - 词条
  parameters:
    - in: path
      name: entry_id
      required: true
      schema:
        type: integer
        default: 1
    - in: path
      name: tag_id
      required: true
      schema:
        type: integer
        default: 1
  responses:
    200:
      $ref: '../responses/200.yaml'
